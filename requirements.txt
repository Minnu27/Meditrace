# MediTrace API/serverless runtime. Keep ML research dependencies in
# requirements-ml.txt so they are not bundled into the Vercel function.
fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
psycopg[binary]>=3.1
PyMySQL[rsa]>=1.1
python-dotenv>=1.0
python-multipart
pypdf>=5.0
requests
gradio

# dev tools (VS Code formatting)
black
pytest

# optional — only needed if you use report_draft.py's draft_report_llm() path
# anthropic
